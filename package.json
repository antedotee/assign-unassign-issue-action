{
  "name": "assign-unassign-issue-action",
  "version": "1.0.0",
  "description": "GitHub Action for assigning/unassigning issues with configurable limits",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist --source-map --license licenses.txt",
    "test": "jest",
    "lint": "eslint src/**/*.js"
  },
  "keywords": [
    "github-action",
    "issue-assignment",
    "automation"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1",
    "eslint": "^8.57.0",
    "jest": "^29.7.0"
  }
}

